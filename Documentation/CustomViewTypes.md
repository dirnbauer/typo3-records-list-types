# Custom View Types

The maintained custom view type guide is
[Documentation/Developer/CustomViewTypes.rst](Developer/CustomViewTypes.rst).

Custom view types inherit the shared workspace-aware search and filter
pipeline used by the module. Templates only need the normal action wrapper;
record matching is performed against the overlaid row data before rendering.

This Markdown file is a compatibility pointer for older links.
